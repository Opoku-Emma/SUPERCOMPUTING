# SUPERCOMPUTING

First change made to file!

Made some more changes. Seriously <- that was the change
```bash
git add .
git commit -m "Message"
git push
```

```bash
git log # check commit history
git stash # store recent changes somewhere
```

```bash
# work with imaginary files
wc -l <(date)

# evaluate commands on the fly
echo "Today is $(date)"

# pattern substitution
# ${variable/pattern/replacement} replace first match
# ${variable/pattern/replacement} replace all matches

VAR1="some_c_h_a_r_c"
echo ${VAR1//c/i}




```
